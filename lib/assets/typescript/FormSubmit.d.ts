import { ReactElement } from 'react';
interface FORM {
    name: string;
}
interface Props {
    uri: string;
    inputsElements: FORM;
}
export default function Example(props: Props): ReactElement;
export {};
